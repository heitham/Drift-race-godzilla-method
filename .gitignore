node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Local git clones of the published site — nested repos, re-clonable on demand
clones/

# Database dumps — large binaries, regenerate with `npm run baseline:dump`
baseline/*.dump

# Per-run working copies (nested git repos, regenerated by the harness)
work/

# Run outputs are large and regenerable; published separately with results.
# Remove this line when publishing a completed run set.
results/
